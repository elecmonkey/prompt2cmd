# Prompt2Cmd 配置文件示例
# 复制此文件为 .env 并修改为你自己的配置

#=====================================================
# API 配置
#=====================================================

# DeepSeek API密钥（必需）
# 从 https://platform.deepseek.com/api_keys 获取
DEEPSEEK_API_KEY=your_api_key_here

# DeepSeek API基础URL（可选，有默认值）
# 默认值: https://api.deepseek.com
DEEPSEEK_BASE_URL=https://api.deepseek.com

#=====================================================
# 应用功能配置 
#=====================================================

# 历史记录最大保存数量（可选，有默认值）
# 默认值: 50
MAX_HISTORY_SIZE=50

# 是否使用本地模型（可选，有默认值）
# 值: true 或 false
# 默认值: false
USE_LOCAL_MODEL=false

# 本地模型路径（仅当USE_LOCAL_MODEL=true时必需）
# 示例: /home/user/models/llm-model
LOCAL_MODEL_PATH=

#=====================================================
# 安全配置
#=====================================================

# 危险命令列表（可选，有默认值）
# 以逗号分隔的命令列表，执行这些命令时会显示额外警告
# 默认值: rm -rf,rm,chmod,chown,mkfs,dd,mv,reboot,shutdown
DANGEROUS_COMMANDS=rm -rf,rm,chmod,chown,mkfs,dd,mv,reboot,shutdown 